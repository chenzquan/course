spring:
  application:
    name: eureka

server:
  port: 8761

eureka:
  client:
    fetch-registry: false                                   #获取注册信息
    register-with-eureka: false                          #是否注册到注册中心去

